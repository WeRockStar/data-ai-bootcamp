# Docker-Compose
## Stop all container
```bash
docker ps -a
docker stop <container_name>
docker rm <container_name>
```
## run docker compose
```bash
docker-compose up -d --build
```

## Congratulation!!